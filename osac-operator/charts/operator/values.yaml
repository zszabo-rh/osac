image:
  repository: ghcr.io/osac-project/osac-operator
  tag: sha-e38c7ab # PLACEHOLDER -- overwritten at release time by publish-charts.yaml
  pullPolicy: Always
replicaCount: 1
resources:
  limits:
    cpu: 500m
    memory: 128Mi
  requests:
    cpu: 10m
    memory: 64Mi
aap:
  url: ""
  token: ""
  insecureSkipVerify: "true"
  statusPollInterval: "30s"
  templatePrefix: "osac"
stall:
  preparingInfrastructureThreshold: "15m"
  controlPlaneStartingThreshold: "30m"
  workersJoiningThreshold: "20m"
  workersJoiningByHostType: {}
fulfillment:
  serverAddress: ""
  tokenFile: ""
controllers:
  tenant: true
  networking: true
  storage: true
  # The Volume controller provisions volumes via the vendor CSI controllers.
  # Safe to leave enabled even without a vendor backend: when vendorControllers
  # is empty the controller starts with provisioning disabled (Volumes stay in
  # Progressing) and never fails operator startup.
  volume: true
  networkingProvisioning: true
# vendorControllers maps StorageBackend names to their vendor CSI controller
# gRPC endpoints, rendered into OSAC_VENDOR_CONTROLLERS for the Volume
# controller. Optional: leave empty on setups with no vendor storage backend
# (e.g. LVMS/dev) — provisioning is disabled until a backend is configured.
# Example:
#   vendorControllers:
#     vast: vast-csi-controller.osac-csi-backends.svc:50051
vendorControllers: {}
configSecret:
  name: "osac-config"
  optional: true
serviceAccount:
  create: true
  name: ""
consoleProxy:
  disabled: false
  vmClusterMode: auto
  resources:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 10m
      memory: 64Mi
certs:
  issuerRef:
    kind: ClusterIssuer
    name: default-ca
remoteClusterKubeconfig: ""
hubAccess:
  enabled: false
tenants: []
# --- Network Manager Registration ---
# ConfigMaps that register networking managers for the two-manager model.
# The operator discovers managers by selecting ConfigMaps with labels
# osac.openshift.io/network-fabric-manager or osac.openshift.io/network-k8s-manager.
networkManagers:
  enabled: true
  # capabilitiesSyncInterval controls how often the operator recomputes NetworkClass
  # capabilities from the fabric/k8s manager ConfigMaps (in addition to reacting
  # immediately to ConfigMap changes).
  capabilitiesSyncInterval: "5m"
  fabricManagers:
    netris:
      enabled: false
      description: >-
        Netris SDN controller for physical fabric management. Manages VLAN/VxLAN segments, ACLs, public IP allocation, and NAT gateways.
      capabilities: "ipv4"
    cudn_net:
      enabled: true
      description: >-
        CUDN-based isolated networking (ClusterUserDefinedNetwork). Leaf overlay role for VirtualNetwork/Subnet — not a physical fabric manager. VMaaS without a fabric uses k8sManagers.k8s_only as the NetworkClass manager, which delegates overlay work here.
      capabilities: "ipv4,ipv6,dualStack"
  k8sManagers:
    k8s_only:
      enabled: false
      description: >-
        Composite k8s-native networking (CUDN, Kubernetes NetworkPolicy, MetalLB L2) with no separate physical fabric. Delegates each resource type to an existing k8s-native implementation — see osac-aap's osac.templates.k8s_only role.
      capabilities: "ipv4,ipv6"
